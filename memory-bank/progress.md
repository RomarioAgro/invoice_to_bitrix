# Progress

## Готово

- Memory Bank инициализирован.
- Реализованы распознавание, Telegram-диалог, Bitrix-клиент, конкурентность, очистка и безопасные логи.
- Добавлены тесты, Dockerfile, Compose, README и example-конфигурации.
- VPS подготовлен: Docker/Xray установлены, контейнер healthy, Telegram доступен через SOCKS5.
- Парсер поддерживает числовые даты и даты с русскими названиями месяцев.
- Реализованы требования ТЗ 2.2: DaData, HTML-сводка, автоматический опрос и необязательный номер задачи.
- Номер задачи сделан обязательным; пустое значение больше не допускается.
- Исправлено извлечение ссылки на сделку из вложенного поля ответа Битрикс `result.link_deal`; добавлен тест реального формата ответа.
- Реализовано описание из Telegram `caption`: краевые пробелы удаляются, внутренние переносы сохраняются; пустая подпись оставляет описание для отдельного опроса.
- Подтверждено тестом, что описание из содержимого PDF/XLSX автоматически не извлекается.
- Реализована fail-closed конфигурация `[access] allowed_user_ids` с дедупликацией положительных Telegram User ID.
- Добавлен централизованный Telegram access guard до команд, файлов, текста и callback; группы не обрабатываются.
- Добавлены тесты разрешённого private update, отказа пользователю, группе и callback; целевой набор содержит 19 тестов.
- Добавлено распознавание ИНН из подписей `ИНН/КПП` и `ИНН / КПП`; регрессионный тест проверяет ИНН продавца и покупателя.
- Исправление `ИНН/КПП` развёрнуто на VPS: production-контейнер `healthy`, перезапусков 0, 20 тестов проходят.
- По явному требованию владельца Xray SOCKS5 переведён с `127.0.0.1:10808` на `0.0.0.0:10808`; конфигурация проверена, служба перезапущена и активна.
- Исправлены шаблоны номера и суммы: окончания слов «счету/счета» не принимаются за номер, а итог к оплате имеет приоритет над суммой без НДС. На `991161.pdf` распознаны `24004-29757` и `2970.00`; 21 тест проходит.
- Добавлен узкий шаблон номера без знака `№` для формата `Счёт на оплату 59709636865`; все четыре примера из `techspec` корректно распознают основные реквизиты, 22 теста проходят.
- Исправления распознавания отправлены в `origin/master` коммитом `08b2fe8` и развёрнуты на VPS; production-контейнер пересобран, 22 теста прошли, healthcheck `healthy`.
- Ручной срок оплаты принимает `ДД,ММ,ГГГГ` с нормализацией в `ДД.ММ.ГГГГ`; дата счёта не изменена, 23 теста проходят.
- Поддержка срока оплаты через запятую отправлена в `origin/master` коммитом `006ba6d` и развёрнута на VPS; 23 теста прошли, контейнер `healthy`, перезапусков 0.

## В работе

- Пользовательский smoke test белого списка разрешённым аккаунтом; опционально — отдельным запрещённым аккаунтом и тестовой группой.

## Известные проблемы

- Локальная Windows `.venv` использует Python 3.10 и не смогла скачать новые зависимости из-за системной proxy-ошибки; полный тест выполнен в целевом Docker Python 3.12.
